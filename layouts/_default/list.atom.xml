{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
  {{- $pages = $pctx.RegularPages -}}
{{- else -}}
  {{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $pages = where $pages "Section" "post" -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
  {{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\"?>" | safeHTML }}
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ if eq .Title .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
  <link href="{{ .Permalink }}" rel="alternate" type="text/html" />
  {{- with .OutputFormats.Get "Atom" }}
  <link href="{{ .Permalink }}" rel="self" type="application/atom+xml" />
  {{- end }}
  <id>{{ .Permalink }}</id>
  {{ if not .Date.IsZero }}<updated>{{ .Date.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</updated>{{ end }}
  <subtitle>Recent content {{ if ne .Title .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</subtitle>
  <generator>Hugo -- gohugo.io</generator>
  {{ range $pages }}
  <entry>
    <title>{{ .Title }}</title>
    <link href="{{ .Permalink }}" rel="alternate" type="text/html" />
    <id>{{ .Permalink }}</id>
    <published>{{ .Date.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</published>
    {{ if not .Lastmod.IsZero }}<updated>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</updated>{{ end }}
    <author><name>{{ .Site.Params.author }}</name></author>
    <content type="html">{{ .Content | replaceRE `(?s)<lite-youtube[^>]*>.*?</lite-youtube>` "" | html }}
    {{ printf " <hr> <p>Thank you for subscribing to my feed. You're awesome.</p> <p><a href=\"mailto:blogrss@bpugh.dev\">Reply by email</a></p>" | html }}
      &lt;img src=&quot;https://blog.bpugh.workers.dev/cdn/images?p={{.RelPermalink}}feed&quot;&gt;
    </content>
  </entry>
  {{ end }}
</feed>